server {
  listen 6301;
  server_name localhost;

  location / {
    root /usr/share/nginx/html/test;
    index index.html;
  }

  location /api {
    proxy_pass http://host.docker.internal:3000/api;
  }
}
